---
title: STM32 + ESP8266 + DHT11 + OLED 温湿度物联网项目
tags:
  - stm32
  - esp8266
  - iot
  - dht11
  - oled
  - 嵌入式
created: 2026-08-08
updated: 2026-08-09
---

# STM32 + ESP8266 + DHT11 + OLED 温湿度物联网项目

> [!info] 一句话概述
> STM32F103C8 通过 USART 驱动 ATK-MW8266D（ESP8266）WiFi 模块，每 5 秒采集 DHT11 温湿度，OLED 本地显示，同时 POST JSON 上传到自建本地服务器看板。

## 硬件清单

| 部件 | 型号 | 说明 |
|---|---|---|
| 主控 | STM32F103C8（蓝色 BluePill） | StdPeriph 标准外设库，Keil V5.06 编译 |
| WiFi 模块 | ATK-MW8266D V1.4 | 本质是 ESP8266，串口 AT 指令通信（非 SPI） |
| 温湿度 | DHT11 | 单总线，精度 ±1°C / ±5%RH |
| 显示 | 0.96" OLED 128×64 | SSD1306（或 SH1106），I2C 地址 0x78 |
| 调试 | USB-TTL 串口工具 | 接 PA9/PA10，115200 8N1 |

## 引脚分配

```
STM32F103C8 引脚分配
┌──────────┬───────────┬──────────────────────┐
│ STM32    │ 外设       │ 说明                  │
├──────────┼───────────┼──────────────────────┤
│ PA2 (TX) │ ESP8266 RXD│ USART2 → WiFi 模块    │
│ PA3 (RX) │ ESP8266 TXD│ WiFi 模块 → USART2    │
│ PA9 (TX) │ USB-TTL RX │ USART1 调试输出       │
│ PA10(RX) │ USB-TTL TX │ USART1 调试输入       │
│ PA0      │ DHT11 DATA │ 单总线，需 4.7k 上拉  │
│ PB8      │ OLED SCL   │ 软件 I2C 时钟         │
│ PB9      │ OLED SDA   │ 软件 I2C 数据         │
│ PC13     │ 板载 LED   │ 运行指示              │
│ 3.3V     │ ESP8266 VCC│ 模块供电              │
│ GND      │ 共地       │ 所有设备共地           │
└──────────┴───────────┴──────────────────────┘
```

> [!tip] RST / IO_0 引脚
> ATK-MW8266D 的 RST 和 IO_0 内部已上拉，正常使用**可不接**。
> - RST：高=正常运行，低=复位
> - IO_0：高=运行模式，低=固件烧录模式
> - 切勿直接接地（会一直复位 / 进烧录模式）

## 软件架构

### 文件结构

```
3-0 WLAN/
├── User/
│   ├── main.c           # 主程序：初始化→连WiFi→循环(采集→显示→上传)
│   ├── delay.c/.h       # SysTick 1ms 时基 + Delay_us（DWT微秒延时）
│   ├── usart.c/.h       # USART1(调试) + USART2(WiFi, 512B环形缓冲+RXNE中断)
│   ├── esp8266.c/.h     # ESP8266 AT 指令驱动
│   ├── dht11.c/.h       # DHT11 单总线温湿度读取
│   ├── oled.c/.h        # OLED SSD1306 软件 I2C 驱动（江协科技版）
│   ├── oled_font.h      # 8x16 ASCII 字库（江协标准 95 字符）
│   └── stm32f10x_it.c   # 中断：SysTick_Handler + USART2_IRQHandler
├── server/
│   ├── app.py           # Python 标准库 HTTP 服务器（零依赖）
│   └── index.html       # Chart.js 双轴温湿度看板
├── Library/             # StdPeriph 标准外设库
├── Start/               # 启动文件
└── Project.uvprojx      # Keil 工程文件
```

### 主程序流程

```
上电
 │
 ├─ PC13 LED 闪烁（确认程序跑起来）
 ├─ ESP8266_Init()          # AT 测试 × 5 + CWMODE=1 + CIPMUX=0
 ├─ 连接 WiFi（重试 2 次 + IsConnected 复检）
 ├─ ESP8266_PrintIP()       # 打印模块 IP
 │
 └─ 主循环（每 5 秒）
     ├─ DHT11_Read()        # 读取温湿度
     ├─ OLED 显示 TEMP/HUMI
     ├─ 自检 WiFi 连接状态
     ├─ ESP8266_HttpPost()  # POST JSON 到本地服务器
     │   ├─ 失败 → CIPCLOSE 关半开 socket → 重试 1 次
     │   └─ 成功 → 打印 [OK] uploaded
     └─ 转发 WiFi 回显到调试串口
```

### 关键配置

| 配置项 | 值 | 位置 |
|---|---|---|
| WiFi SSID | `YOUR_SSID` | main.c |
| WiFi 密码 | `YOUR_PASSWORD` | main.c |
| 服务器 IP | `YOUR_LOCAL_IP` | main.c `#define SERVER_IP` |
| 服务器端口 | `8000` | main.c `#define SERVER_PORT` |
| 服务器路径 | `/api/data` | main.c `#define SERVER_PATH` |
| 上传间隔 | 5 秒 | main.c `Delay_ms(5000)` |
| 调试波特率 | 115200 | usart.c |
| ESP8266 波特率 | 115200 | usart.c |
| printf | 需勾选 MicroLIB | Keil → Options → Target |

> [!warning] SERVER_IP 必须是真实局域网 IP
> ESP8266 是另一台设备，**不能用** `localhost` 或 `127.0.0.1`。
> 查法：`cmd` → `ipconfig` → 找"无线局域网适配器 WLAN"下的 IPv4 地址。
> 如果开了 VPN，先关掉——VPN 虚拟网卡的 IP（如 `26.x.x.x`）不是局域网 IP。

## ESP8266 驱动 API

| 函数 | 说明 | 关键超时 |
|---|---|---|
| `ESP8266_Init()` | 初始化模块（AT/CWMODE/CIPMUX） | AT 重试 5 次 |
| `ESP8266_JoinAP(ssid, pwd)` | 连接路由器 | 20 秒 |
| `ESP8266_IsConnected()` | 查询是否已连 AP | 2 秒 |
| `ESP8266_PrintIP()` | 打印模块 IP（AT+CIFSR） | 1 秒 |
| `ESP8266_ConnectTCP(ip, port)` | 建立 TCP 连接 | 8 秒 × 2 重试 |
| `ESP8266_SendTCP(data, len)` | 发送数据（CIPSEND） | 2 秒等 `>` |
| `ESP8266_HttpPost(host, path, port, body)` | POST JSON | 含 ConnectTCP + SendTCP |
| `ESP8266_CloseTCP()` | 关闭 TCP 连接 | 2 秒 |

### AT 指令流程

```
AT              → OK
AT+CWMODE=1     → OK          # Station 模式
AT+CIPMUX=0     → OK          # 单连接模式
AT+CWJAP="SSID","PASSWORD"    # 连 WiFi（最慢 20 秒）
                 → WIFI CONNECTED / WIFI GOT IP / OK

# 每次上传：
AT+CIPSTART="TCP","YOUR_LOCAL_IP","8000"  → CONNECT / OK
AT+CIPSEND=128                            → >
<HTTP POST 报文>                           → SEND OK
AT+CIPCLOSE                               → CLOSED
```

## DHT11 驱动

- **总线**：PA0 单总线，需 4.7k 上拉电阻（多数模块板载）
- **时序**：起始信号（拉低 18ms）→ 响应握手 → 40bit 数据 → 校验和
- **延时**：基于 DWT 周期计数器（`Delay_us`），需 `Delay_Init()` 使能
- **API**：`DHT11_Init()` + `DHT11_Read(int8_t *temp, int8_t *humi)`

> [!note] DWT 寄存器定义
> 本项目旧版 `core_cm3.h` 未定义 DWT，需在 `delay.h` 手动补：
> ```c
> #ifndef DWT
> #define DWT_BASE   (0xE0001000u)
> typedef struct { volatile uint32_t CTRL, CYCCNT, CPICNT, EXCCNT, ... } DWT_Type;
> #define DWT ((DWT_Type*)DWT_BASE)
> #define DWT_CTRL_CYCCNTENA_Msk (1u)
> #define CoreDebug_DEMCR_TRCENA_Msk (1u << 24)
> #endif
> ```

## OLED 驱动

- **芯片**：SSD1306（128×64），I2C 地址 0x78
- **总线**：PB8(SCL) / PB9(SDA)，软件 I2C
- **字库**：江协科技标准 8×16 ASCII 字库（32~126 共 95 字符）
- **取模约定**：列优先、byte[0..7] 上半 / byte[8..15] 下半、bit7 为顶
- **API**：`OLED_Init()` / `OLED_Clear()` / `OLED_ShowString()` / `OLED_ShowNum()`

> [!warning] SSD1306 vs SH1106
> 廉价 0.96" 屏常实为 SH1106，用 SSD1306 驱动会整屏错乱。
> 区分方法：看屏背面黑色芯片丝印。SH1106 的显存是 132 列（多 4 列偏移），
> 需改 `oled.c` 初始化序列和列地址设置。
> **若换字库后仍乱码，先确认芯片型号。**

## 本地服务器看板

### 架构

```
STM32 ──POST JSON──→ Python HTTP Server (8000)
                          │
                     ┌────┴────┐
                     │         │
                 data.csv    index.html
                 (持久化)    (Chart.js 曲线)
```

### 服务器端（server/app.py）

- **零依赖**：纯 Python 标准库 `http.server`，不用 Flask
- **接口**：
  - `POST /api/data` — 收 `{"t":30,"h":60}`，返回 `{"ok":true}`
  - `GET /api/history` — 返回最近 720 个数据点
  - `GET /api/latest` — 返回最新一条
  - `GET /` — 返回看板页面
- **存储**：内存环形缓冲（720 点）+ 追加写 `data.csv`
- **启动**：`python app.py`，自动打印本机局域网 IP

### 前端看板（server/index.html）

- Chart.js 走 CDN（只在浏览器加载，不落盘）
- 双 Y 轴折线（左轴温度 / 右轴湿度）
- 大数字卡片显示当前值
- 每 2 秒自动刷新

### 运行步骤

1. 改 `main.c` 第 15 行 `SERVER_IP` 为你电脑局域网 IP
2. Keil 全量 Rebuild → 下载
3. 在 `server/` 目录运行 `python app.py`
4. 浏览器打开 `http://localhost:8000/`
5. STM32 上电，看板出现实时曲线

## Keil 工程配置要点

| 配置项 | 值 | 位置 |
|---|---|---|
| Use MicroLIB | ✅ 勾选 | Options → Target |
| 编译器 | ARMCC V5.06 | 默认 |
| 源文件组 | User 组包含所有 .c | Project → Manage → Items |
| 全量重建 | Project → Rebuild | **不要用 Build（增量）** |

> [!danger] 必须全量 Rebuild
> 改了字库（oled_font.h）或头文件后，增量编译会用旧的 .o 缓存，
> 等于没改。必须 **Project → Rebuild all target files** 全量重建。

## 调试串口日志判读

| 日志关键词 | 含义 | 是否正常 |
|---|---|---|
| `OK` | 模块响应正常 | ✅ |
| `WIFI CONNECTED` / `WIFI GOT IP` | 模块连上路由并拿到 IP | ✅ |
| `[OK] WiFi connected` | 程序确认 WiFi 就绪 | ✅ |
| `IP: 192.168.x.x` | 模块自身 IP | ✅ |
| `Temp=30C Humi=60%` | DHT11 读取成功 | ✅ |
| `upload -> YOUR_LOCAL_IP:8000/api/data` | 上传目标正确 | ✅ |
| `[OK] uploaded (try 1)` | POST 成功 | ✅ |
| `[ERR] upload failed (try 1)` | 第一次失败，正在重试 | ⚠️ 看 try 2 |
| `busy p...` | 模块忙，断电重启 | ❌ |
| `+CWJAP:3` | 找不到 AP | ❌ 检查 SSID/频段 |
| `ERROR` | AT 指令出错 | ❌ |
| `IP:` 后面空白 | PrintIP 读取时序问题 | ⚠️ 不影响功能 |

## 扩展方向

| 方向 | 说明 | 状态 |
|---|---|---|
| ~~HTTP POST 验证~~ | postman-echo 200 + JSON 回显 | ✅ 已完成 |
| ~~自建本地服务器~~ | Python + Chart.js 看板 | ✅ 已完成 |
| MQTT + 平台看板 | ThingsBoard / EMQX | ⬜ 待定 |
| 低功耗电池节点 | Standby + RTC 唤醒 | ⬜ 待定 |
| OLED 滚动曲线 | 屏上画历史折线 | ⬜ 待定 |
| 运行时配网 | 串口输入 / SmartConfig | ⬜ 待定 |

## 相关笔记

- [[ESP8266开发踩坑笔记]]
- [[WLAN调试指南]]
